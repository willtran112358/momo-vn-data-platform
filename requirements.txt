# MoMo VN Data Platform — sample dependencies (educational)
# Samples are designed to run with DRY_RUN=1 without external clusters.

# Core
pyyaml==6.0.2
python-dateutil==2.9.0.post0

# Data / ML samples (optional for full runs)
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2

# Orchestration sample (DAG is illustrative; install only if you wire Airflow)
# apache-airflow==2.10.3

# Streaming sample (PyFlink is heavy; install only for real Flink runs)
# apache-flink==1.20.0
